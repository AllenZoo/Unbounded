using UnityEngine;

/// <summary>
/// Attach this MonoBehaviour to any UI element that should block gameplay input when active. 
/// This is used to prevent the player from moving or attacking while interacting with UI elements like buttons.
/// </summary>
public class BlockGameplayInput : MonoBehaviour
{
   
}
